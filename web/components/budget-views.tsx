"use client";
import {useMemo,useState} from "react";
import {ArrowRight,ArrowUpRight,Check,ChevronLeft,ChevronRight,CreditCard,FileSpreadsheet,Loader2,Repeat2,Search,Send,Sparkles,Upload} from "lucide-react";
import {Dialog,DialogContent,DialogDescription,DialogHeader,DialogTitle} from "@/components/ui/dialog";
import {Sheet,SheetContent,SheetDescription,SheetHeader,SheetTitle} from "@/components/ui/sheet";
import {Select,SelectContent,SelectItem,SelectTrigger,SelectValue} from "@/components/ui/select";
import {Input} from "@/components/ui/input";
import {Textarea} from "@/components/ui/textarea";
import {Table,TableBody,TableCell,TableHead,TableHeader,TableRow} from "@/components/ui/table";
import {money,shortDate,sourceNames,type Transaction,type Source} from "@/lib/transactions";
import {parseStatement} from "@/lib/import-csv";
import {detectRecurring,type Recurring} from "@/lib/recurring";
import {MerchantIcon,TransactionTable} from "./transaction-table";
export function TransactionsView({rows,onImport}:{rows:Transaction[];onImport:()=>void}) {
 const [query,setQuery]=useState(""),[category,setCategory]=useState("all"),[page,setPage]=useState(0),[sort,setSort]=useState("newest");
 const categories=[...new Set(rows.map(t=>t.category))].sort();
 const filtered=rows.filter(t=>(category==="all"||t.category===category)&&`${t.merchant} ${t.category} ${t.date}`.toLowerCase().includes(query.toLowerCase())).sort((a,b)=>sort==="amount"?b.amount-a.amount:sort==="oldest"?a.date.localeCompare(b.date):b.date.localeCompare(a.date));
 const pages=Math.max(1,Math.ceil(filtered.length/15)),currentPage=Math.min(page,pages-1);
 return <section className="panel"><div className="transaction-tools"><div className="search-field"><Search size={17}/><Input aria-label="Search transactions" placeholder="Search merchants or transactions…" value={query} onChange={e=>{setQuery(e.target.value);setPage(0);}}/></div><Select value={category} onValueChange={v=>{setCategory(v);setPage(0);}}><SelectTrigger aria-label="Filter category"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="all">All categories</SelectItem>{categories.map(c=><SelectItem key={c} value={c}>{c}</SelectItem>)}</SelectContent></Select><Select value={sort} onValueChange={setSort}><SelectTrigger aria-label="Sort transactions"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="newest">Newest first</SelectItem><SelectItem value="oldest">Oldest first</SelectItem><SelectItem value="amount">Largest first</SelectItem></SelectContent></Select></div>{filtered.length?<TransactionTable rows={filtered.slice(currentPage*15,currentPage*15+15)}/>:<Empty title="No transactions found" text="Try another search or import a statement for this period." onImport={onImport}/>}<div className="table-pagination"><span>{filtered.length} transactions · payments excluded from spending totals</span><div><button aria-label="Previous page" disabled={!currentPage} onClick={()=>setPage(currentPage-1)}><ChevronLeft size={17}/></button><span>{currentPage+1} / {pages}</span><button aria-label="Next page" disabled={currentPage>=pages-1} onClick={()=>setPage(currentPage+1)}><ChevronRight size={17}/></button></div></div></section>;
}
function Empty({title,text,onImport}:{title:string;text:string;onImport:()=>void}) {return <div className="empty-state"><FileSpreadsheet size={32}/><h2>{title}</h2><p>{text}</p><button className="primary-button" onClick={onImport}><Upload size={16}/>Import a statement</button></div>;}
export function RecurringView({rows,onImport}:{rows:Transaction[];onImport:()=>void}) {
 const recurring=useMemo(()=>detectRecurring(rows),[rows]),[detail,setDetail]=useState<Recurring|null>(null),active=recurring.filter(r=>!r.overdue),monthly=active.reduce((s,r)=>s+r.monthly,0);
 return <><section className="stats-grid"><div className="stat-card"><div className="stat-label">Estimated monthly cost<Repeat2 size={18}/></div><div className="stat-number">{money(monthly)}</div><div className="stat-foot">Active patterns, normalized to a month</div></div><div className="stat-card"><div className="stat-label">Recurring patterns</div><div className="stat-number">{recurring.length}</div><div className="stat-foot">{active.length} current · {recurring.length-active.length} need review</div></div><div className="stat-card"><div className="stat-label">Estimated yearly cost</div><div className="stat-number">{money(monthly*12)}</div><div className="stat-foot">Assuming current patterns continue</div></div></section><section className="panel"><div className="panel-heading"><div><h2>Familiar faces in your statements</h2><p>Estimates from at least two charges with similar amounts and regular timing.</p></div></div>{recurring.length?<Table><TableHeader><TableRow><TableHead>Merchant</TableHead><TableHead>Frequency</TableHead><TableHead>Next expected</TableHead><TableHead>Evidence</TableHead><TableHead className="amount">Last charge</TableHead></TableRow></TableHeader><TableBody>{recurring.map(r=><TableRow key={r.id}><TableCell><button className="merchant merchant-detail" onClick={()=>setDetail(r)}><MerchantIcon name={r.merchant}/><span><strong>{r.merchant}</strong><small>{r.history[0]?.accountName||sourceNames[r.source]}</small></span></button></TableCell><TableCell>{r.cadence}</TableCell><TableCell>{shortDate(r.nextDate)}<small className={r.overdue?"review-note":"muted"}>{r.overdue?"Overdue · check history":"Estimated"}</small></TableCell><TableCell><span className={`confidence ${r.confidence==="Likely"?"likely":""}`}>{r.confidence}</span><small className="muted">{r.count} charges found</small></TableCell><TableCell className="amount">{money(r.amount)}</TableCell></TableRow>)}</TableBody></Table>:<Empty title="A little history goes a long way" text="Connect a card or import two or more billing cycles to find likely recurring payments. Charges that vary by more than 20% may not be detected." onImport={onImport}/>}<p className="panel-note">These are detected patterns, not confirmed subscriptions. Irregular bills, missing months, and new subscriptions may not appear. Select a merchant to review its history.</p></section><Sheet open={!!detail} onOpenChange={open=>!open&&setDetail(null)}><SheetContent className="detail-sheet"><SheetHeader><SheetTitle>{detail?.merchant}</SheetTitle><SheetDescription>{detail?.cadence} pattern · {detail?.confidence?.toLowerCase()} recurring payment</SheetDescription></SheetHeader><div className="sheet-body"><p>Next expected: <strong>{detail&&shortDate(detail.nextDate)}</strong>. Timing is estimated from previous charges.</p><h3>Supporting transactions</h3>{detail?.history.map(t=><div className="history-row" key={t.id}><span>{t.date}</span><strong>{money(t.amount)}</strong></div>)}</div></SheetContent></Sheet></>;
}
export function InsightsView({aiConfigured,month,source,provider}:{aiConfigured:boolean;month:string;source:string;provider:"csv"|"plaid"}) {
 const [question,setQuestion]=useState(""),[busy,setBusy]=useState(false),[error,setError]=useState(""),[messages,setMessages]=useState<{question:string;answer:string;engine:string;context:string}[]>([]);
 async function ask(value:string){if(!value.trim()||busy)return;setBusy(true);setError("");try{const response=await fetch("/api/insights",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({question:value,month,source,provider})});const data=await response.json() as {error?:string;answer:string;engine:string};if(!response.ok)throw Error(data.error);setMessages(m=>[...m,{question:value,answer:data.answer,engine:data.engine,context:`${month==="all"?"All time":month} · ${source==="all"?"All accounts":sourceNames[source as Source]} · ${provider==="plaid"?"Connected cards":"CSV statements"}`}]);setQuestion("");}catch(e){setError(e instanceof Error?e.message:"Could not get an answer.");}finally{setBusy(false);}}
 return <section className="panel insights-view"><div className="insights-intro"><span className="insight-symbol"><Sparkles size={24}/></span><span className="engine-tag">{aiConfigured?"AI connected":"Instant analysis · AI not connected"}</span><h2>A clearer picture starts<br/>with a good question.</h2><p>{aiConfigured?"Ask about your spending and recurring payments. Your question and a transaction summary are sent to OpenAI when you ask.":"Explore calculated summaries now. Live AI answers will become available once an AI connection is configured."}</p></div><div className="suggestions">{["Where did my money go?","What payments are recurring?","Compare my cards","How has my spending changed?"].map(q=><button key={q} disabled={busy} onClick={()=>ask(q)}><Sparkles size={15}/>{q}<ArrowUpRight size={15}/></button>)}</div><div className="chat-messages" aria-live="polite">{messages.map((m,i)=><div className="chat-exchange" key={i}><div className="user-message">{m.question}</div><div className="assistant-message"><span className="answer-label"><Sparkles size={15}/>{m.engine==="openai"?"Ledger AI":"Instant analysis"}<small>{m.context}</small></span><p>{m.answer}</p></div></div>)}{busy&&<div className="thinking"><Loader2 size={18} className="animate-spin"/>Looking through your spending…</div>}</div>{error&&<p className="form-error" role="alert">{error}</p>}<form className="ask-form" onSubmit={e=>{e.preventDefault();void ask(question);}}><Input aria-label="Ask about your spending" placeholder="Ask about your spending…" value={question} onChange={e=>setQuestion(e.target.value)} maxLength={1000}/><button className="primary-button" aria-label="Send question" disabled={busy||!question.trim()}><Send size={17}/></button></form><p className="insights-footnote">Answers reflect the selected period and account. Recurring analysis uses all available history.</p></section>;
}
export function ImportDialog({open,onOpenChange,onImported}:{open:boolean;onOpenChange:(open:boolean)=>void;onImported:(rows:Transaction[],added:number,duplicates:number)=>void}) {
 const [source,setSource]=useState<Source>("amex"),[csv,setCsv]=useState(""),[filename,setFilename]=useState(""),[negative,setNegative]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState("");
 const parsed=useMemo(()=>{if(!csv)return {rows:[],error:""};try{return {rows:parseStatement(csv,source,negative),error:""};}catch(e){return {rows:[],error:e instanceof Error?e.message:"Invalid CSV."};}},[csv,source,negative]);
 async function commit(){setBusy(true);setError("");try{const response=await fetch("/api/transactions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({source,csv,negativePurchases:negative})});const data=await response.json() as {error?:string;transactions:Transaction[];added:number;duplicates:number};if(!response.ok)throw Error(data.error);onImported(data.transactions,data.added,data.duplicates);setCsv("");setFilename("");}catch(e){setError(e instanceof Error?e.message:"Import failed. Your file is still here to retry.");}finally{setBusy(false);}}
 return <Dialog open={open} onOpenChange={value=>{if(!busy)onOpenChange(value);}}><DialogContent className="import-dialog"><DialogHeader><DialogTitle>Bring your transactions together</DialogTitle><DialogDescription>Import a CSV statement from American Express or Discover. Review the amounts before saving.</DialogDescription></DialogHeader><div className="import-options"><label>Card<Select value={source} onValueChange={v=>setSource(v as Source)}><SelectTrigger aria-label="Statement card"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="amex">American Express</SelectItem><SelectItem value="discover">Discover</SelectItem></SelectContent></Select></label><label>Purchase amounts<Select value={negative?"negative":"positive"} onValueChange={v=>setNegative(v==="negative")}><SelectTrigger aria-label="Purchase amount convention"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="positive">Positive in CSV</SelectItem><SelectItem value="negative">Negative in CSV</SelectItem></SelectContent></Select></label></div><label className="upload-zone"><FileSpreadsheet size={30}/><strong>{filename||"Choose your CSV statement"}</strong><span>CSV up to 2 MB · 5,000 rows</span><input type="file" accept=".csv,text/csv" aria-label="Choose CSV statement" disabled={busy} onChange={async e=>{const file=e.target.files?.[0];if(!file)return;setError("");if(file.size>2000000){setError("Please choose a file smaller than 2 MB.");return;}setFilename(file.name);setCsv(await file.text());e.target.value="";}}/></label><details><summary>Or paste CSV text</summary><Textarea aria-label="Paste CSV text" placeholder="Paste your CSV statement here" value={csv} onChange={e=>{setCsv(e.target.value);setFilename("");}} className="csv-textarea"/></details>{(parsed.error||error)&&<p className="form-error" role="alert">{error||parsed.error}</p>}{parsed.rows.length>0&&<div className="import-preview"><div className="preview-heading"><Check size={16}/>{parsed.rows.length} transactions ready to review</div><div className="preview-scroll"><Table><TableHeader><TableRow><TableHead>Date</TableHead><TableHead>Description</TableHead><TableHead className="amount">Amount</TableHead></TableRow></TableHeader><TableBody>{parsed.rows.slice(0,4).map((t,i)=><TableRow key={i}><TableCell>{shortDate(t.date)}</TableCell><TableCell>{t.merchant}<small className="muted">{t.kind}</small></TableCell><TableCell className="amount">{money(t.amount)}</TableCell></TableRow>)}</TableBody></Table></div><p>Purchases should be positive. Refunds should be negative. Card payments are kept separate.</p></div>}<p className="import-privacy">Saving stores these transactions in your private workspace. Existing matches are skipped. CSV statements stay in their own view, separate from connected cards.</p><button className="primary-button" disabled={busy||!parsed.rows.length} onClick={commit}>{busy?<Loader2 className="animate-spin" size={16}/>:<Upload size={16}/>} {busy?"Saving transactions…":"Import transactions"}</button></DialogContent></Dialog>;
}
